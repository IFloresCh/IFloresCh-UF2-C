#include "function.h"
int validacion(int n){
    float n, m_apr, m_rep, i;
    int q_apr, q_rep;

    do{
        printf("introduce una nota: ");
        scanf("%f", &n);

    }while(n > 10 || n < 0;
    return n;
}
float promedio (int n){
    int i;
}