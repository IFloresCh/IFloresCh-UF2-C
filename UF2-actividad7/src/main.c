#include <stdio.h>

void main(){
  int n;
  n= validate(n);
}

/*  int i=0, size, n=0, num, arr[size];
  size = cantidad(n);
  for(i=0; i < size;i++){
      n=validate(n);
      arr[i]= n;
   }
    for(i=0; i < size;i++){
      printf("%d", arr[i]);
  }

*/






